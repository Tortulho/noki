# How to use noki

1º:
Compile with makefile using tags optimized for -O3 or debug for -g

2º:
You can run in REPL mode or File Reading (.noki) mode using ./nokifer or ./nokifer-debug

Example:

REPL mode: ./nokifer

FR mode: ./nokifer filename

More about things related to NOKI on manuals folder
